[Service]
# Supersede the named development-board audio wrapper after an OTA supplies
# native kiosk audio. Never inherit a cross-deployment library search path.
UnsetEnvironment=LD_LIBRARY_PATH ALSA_CONFIG_PATH ALSA_CONFIG_DIR
ExecStart=
ExecStart=/usr/bin/dd-kiosk-browser
