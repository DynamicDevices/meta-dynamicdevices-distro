[Service]
# Run as root even though weston.service uses User=weston. The guard removes
# only the named legacy kiosk hotpatch and otherwise fails closed.
ExecStartPre=+/usr/libexec/dd-kiosk-linker-guard
