# Bind the FT5626 input coordinates to the DSI panel. The panel and controller
# both use the native portrait orientation, so the calibration is identity.
SUBSYSTEM=="input", KERNEL=="event*", ATTRS{name}=="generic ft5x06*", ENV{WL_OUTPUT}="DSI-1", ENV{LIBINPUT_CALIBRATION_MATRIX}="1 0 0 0 1 0"
