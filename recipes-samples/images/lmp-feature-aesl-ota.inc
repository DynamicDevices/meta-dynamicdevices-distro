# AESL-capable clients (DISTRO_FEATURES aesl-ota)
#
# Shared aklite/composectl bumps are in :aesl-ota bbappends.
# Per-factory gateway / DEVICE_API / TUF / token → private meta-subscriber-overrides
# (e.g. active-esl/factory-aesl-lab-meta-subscriber-overrides).
